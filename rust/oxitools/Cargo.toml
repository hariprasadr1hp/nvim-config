[package]
name = "oxitools"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
nvim-oxi = { version = "0.6", features = ["neovim-nightly"] }
chrono = { version = "0.4", features = ["clock"] }
